import { all, takeLatest } from 'redux-saga/effects';

// import { sagaEntity } from './index';
// import {} from '../../action/applicant';
// import {getApplicantsListApi, getApplicantInfoApi, updataApplicantStatusApi} from '../../../../api/index';
import {} from '../../../../api/apiTypes';

export default function* applicantSaga() {
    yield all([
        // takeLatest(),
    ])
}